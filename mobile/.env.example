# Backend URLs the Expo app reads (via app.config.ts -> extra).
# On a physical device, use your machine's LAN IP instead of localhost.
EXPO_PUBLIC_API_URL=http://localhost:8080
EXPO_PUBLIC_WS_URL=ws://localhost:8081
